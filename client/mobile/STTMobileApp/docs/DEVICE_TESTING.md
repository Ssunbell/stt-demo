# 📱 실제 기기에서 테스트하기

실제 iOS/Android 기기에서 앱을 테스트하는 방법입니다.

## 🔍 1. 컴퓨터의 로컬 IP 확인

### macOS/Linux
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

또는 간단하게:
```bash
ipconfig getifaddr en0  # WiFi
ipconfig getifaddr en1  # 이더넷
```

### Windows
```bash
ipconfig
```

**예시 출력**: `192.168.1.100` (이것이 로컬 IP입니다)

## 📝 2. config.ts 파일 수정

`src/utils/config.ts` 파일을 열고 다음 부분을 수정하세요:

### 방법 A: 직접 IP 지정 (권장)

```typescript
// 실제 기기에서 테스트할 때는 이 주소를 컴퓨터의 로컬 IP로 변경하세요
const DEV_SERVER_HOST = '192.168.1.100'; // 여기에 로컬 IP 입력!
```

### 방법 B: 환경 변수 사용

또는 더 유연하게:

```typescript
const getDevServerHost = () => {
  // 실제 기기 테스트를 위한 로컬 IP (필요 시 변경)
  const REAL_DEVICE_IP = '192.168.1.100';
  
  if (__DEV__ && REAL_DEVICE_IP) {
    // 실제 기기에서 테스트 중
    return REAL_DEVICE_IP;
  }
  
  if (Platform.OS === 'ios') {
    return 'localhost';
  } else if (Platform.OS === 'android') {
    return '10.0.2.2';
  }
  
  return 'localhost';
};
```

## ✅ 3. 네트워크 설정 확인

### 중요! 같은 WiFi 네트워크 사용

- ✅ 컴퓨터와 스마트폰이 **같은 WiFi** 네트워크에 연결되어 있어야 합니다
- ❌ 다른 네트워크면 연결되지 않습니다

### 방화벽 확인

**macOS:**
```bash
# 포트 8000 허용 확인
sudo lsof -i :8000
```

**Windows:**
방화벽 설정에서 포트 8000을 인바운드 규칙에 추가하세요.

## 🚀 4. 앱 실행

### Expo Go 앱 사용 (가장 쉬움)

1. 스마트폰에 **Expo Go** 앱 설치
   - iOS: App Store
   - Android: Google Play

2. 터미널에서:
```bash
npm start
```

3. QR 코드 스캔
   - iOS: 카메라 앱으로 스캔
   - Android: Expo Go 앱에서 스캔

4. 앱이 자동으로 열립니다!

### 개발 빌드 사용

```bash
# iOS
npm run ios

# Android  
npm run android
```

## 🔍 5. 연결 테스트

### 서버가 실행 중인지 확인

```bash
# 터미널에서
curl http://localhost:8000/health
```

또는 브라우저에서:
```
http://localhost:8000
```

### 스마트폰에서 서버 접근 테스트

스마트폰 브라우저에서:
```
http://192.168.1.100:8000
```
(로컬 IP로 변경)

서버 응답이 있으면 ✅ 네트워크 연결 성공!

## ❗ 문제 해결

### "Cannot connect to server" 오류

**체크리스트:**
- [ ] 서버가 실행 중인가요? (`python main.py`)
- [ ] 같은 WiFi 네트워크인가요?
- [ ] 로컬 IP가 올바른가요?
- [ ] 방화벽이 포트를 막고 있나요?
- [ ] `config.ts`의 IP 주소가 올바른가요?

### iOS에서 "Insecure connection" 경고

개발 중에는 HTTP를 허용하도록 설정되어 있어야 합니다.
`app.json`에서 확인:

```json
"ios": {
  "infoPlist": {
    "NSAppTransportSecurity": {
      "NSAllowsArbitraryLoads": true
    }
  }
}
```

⚠️ 프로덕션에서는 HTTPS/WSS 필수!

### Android에서 연결 안 됨

1. **네트워크 보안 설정 확인**
2. **앱 권한 확인** (인터넷 권한)
3. **Android 에뮬레이터**: `10.0.2.2` 사용
4. **실제 기기**: 로컬 IP 사용

## 📊 연결 상태 확인

앱 실행 시 콘솔에서 다음 로그를 확인하세요:

```
🔌 Connecting to: ws://192.168.1.100:8000/ws/stt
[INFO] WebSocket: Connected
```

연결 성공! 🎉

## 💡 팁

### 로컬 IP가 자주 바뀌나요?

공유기 설정에서 **DHCP 예약**을 설정하면 컴퓨터의 IP가 고정됩니다.

### 여러 기기에서 테스트

여러 개발자가 테스트하는 경우:

```typescript
// 팀원별 IP 설정
const TEAM_IPS = {
  'lucas-mac': '192.168.1.100',
  'john-pc': '192.168.1.101',
  'sarah-laptop': '192.168.1.102',
};

// 자동 감지 또는 수동 선택
const DEV_SERVER_HOST = TEAM_IPS['lucas-mac'];
```

## 🔐 보안 참고

- **개발 중에만** 로컬 IP 노출
- **프로덕션**에서는 반드시 HTTPS/WSS 사용
- 민감한 데이터는 암호화

---

**문제가 계속되나요?** [FAQ.md](./FAQ.md)를 확인하거나 GitHub Issues에 문의하세요.

